(ocamllex
 (modules ixlexer))

(menhir
 (modules ixparser))

(env
 (dev
  (flags
   ;; disable the stupid setting that,
   ;;  - creates compilation errors for unused variables
   (:standard -warn-error -A))))

(library
 (name ixc)
 (libraries otoml)
 (preprocess
  (pps ppx_deriving.show)))
